<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GymApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymManagement"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Login Screen — entry point (Google Sign-In) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding — create gym after first sign-in -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main app screen (post onboarding) -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- Staff portal — for RECEPTIONIST / TRAINER logins -->
        <activity
            android:name=".ui.staff.StaffDashboardActivity"
            android:exported="false" />

        <!-- Member portal — for gym member self-service logins -->
        <activity
            android:name=".ui.member.MemberPortalActivity"
            android:exported="false" />

        <!-- Progress tracking — weight/BMI/measurements + goals.
             Used both by staff (viewing a specific member) and by members (self-service). -->
        <activity
            android:name=".ui.progress.ProgressActivity"
            android:exported="false" />

    </application>
</manifest>
